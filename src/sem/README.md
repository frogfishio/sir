# SEM - Emulator toolchain for SIR

uses semcore library to run code, feeds it code, and it executes. It also provides an ABI, which is likely going to be a different library e.g. zingcor zi_*

TODO: write full spec