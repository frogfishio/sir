unit bad_strict_mnemonic_count_ignored target host

fn main() -> i32
  ;; 'count' is meaningful only for a few constructs (e.g. alloca); reject it on generic mnemonics in --strict.
  return i32.add(1:i32, 2:i32) count 0:i32
end

