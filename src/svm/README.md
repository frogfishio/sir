# SVM

SIR virtual machine: semcore + JIT, all sorts of things e.g. memory management, garbage collection, like V8 for javascript, but fast and modern

both as a library and as integrated toolchain. Integrated toolchain is likely going to be "sem" or more likely a variant of "sem"

TODO: write full spec